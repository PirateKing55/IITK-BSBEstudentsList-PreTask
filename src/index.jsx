//
import React from "react";
import * as ReactDOM from "react-dom/client";

import App from "./App";

const divContainer = document.getElementById("root");

// Rendering App element in react root :-
ReactDOM.createRoot(divContainer).render(<App />);
